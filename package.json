{
  "name": "freepianoweb",
  "version": "1.0.0",
  "private": true,
  "description": "Tests unitarios de index.html (harness vm, cero dependencias, el HTML permanece como archivo único)",
  "scripts": {
    "test": "node --test test/",
    "smoke": "playwright test --config=smoke/playwright.config.js",
    "smoke:yamaha": "playwright test --config=smoke/playwright.config.js smoke/yamaha.spec.js --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "axe-core": "^4.13.0"
  }
}
